services:
  hazelcast1:
    image: hazelcast/hazelcast:5.6
    container_name: hz1
    ports:
      - "5701:5701"
    volumes:
      - ./hazelcast.yaml:/opt/hazelcast/hazelcast.yaml
    environment:
      - HZ_CLUSTERNAME=dev

  hazelcast2:
    image: hazelcast/hazelcast:5.6
    container_name: hz2
    ports:
      - "5702:5701"
    volumes:
      - ./hazelcast.yaml:/opt/hazelcast/hazelcast.yaml
    environment:
      - HZ_CLUSTERNAME=dev

  hazelcast3:
    image: hazelcast/hazelcast:5.6
    container_name: hz3
    ports:
      - "5703:5701"
    volumes:
      - ./hazelcast.yaml:/opt/hazelcast/hazelcast.yaml
    environment:
      - HZ_CLUSTERNAME=dev

  python-client:
    build: .
    container_name: hz-client
    depends_on:
      - hazelcast1
      - hazelcast2
      - hazelcast3
    environment:
      - PYTHONUNBUFFERED=1
